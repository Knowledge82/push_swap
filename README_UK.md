*Цей проєкт створено в рамках навчальної програми 42 користувачем vdarsuye*

> 🇬🇧 [English](README.md) | 🇺🇦 [Українська](README_UK.md) | 🇷🇺 [Русский](README_RU.md) | 🇪🇸 [Español](README_ES.md)

# 🔄 Push_swap

## 📋 Опис

**Push_swap** — проєкт з алгоритмів сортування, в якому потрібно відсортувати стек цілих чисел, використовуючи обмежений набір операцій з мінімальною кількістю ходів.

Проєкт складається з двох програм:
- **push_swap**: Генерує послідовність інструкцій для сортування стека
- **checker** (бонус): Перевіряє, чи коректно послідовність інструкцій сортує стек

### Завдання

У тебе є два стеки:
- **Стек A**: Містить початкові невідсортовані числа
- **Стек B**: Спочатку порожній, використовується як допоміжне сховище

Можна використовувати тільки ці операції: 

| Операція | Опис |
|----------|------|
| `sa` | Поміняти місцями перші 2 елементи стека A |
| `sb` | Поміняти місцями перші 2 елементи стека B |
| `ss` | Виконати `sa` та `sb` одночасно |
| `pa` | Перемістити верхній елемент з B в A |
| `pb` | Перемістити верхній елемент з A в B |
| `ra` | Ротація стека A (перший елемент стає останнім) |
| `rb` | Ротація стека B (перший елемент стає останнім) |
| `rr` | Виконати `ra` та `rb` одночасно |
| `rra` | Зворотна ротація A (останній елемент стає першим) |
| `rrb` | Зворотна ротація B (останній елемент стає першим) |
| `rrr` | Виконати `rra` та `rrb` одночасно |

---

## 🧠 Алгоритм: Turk Sort

У цій реалізації використовується алгоритм **Turk Sort** — ефективний підхід, що мінімізує загальну кількість операцій.

### Як це працює

#### Фаза 1: Переміщення в стек B (за спаданням)

1. Переміщуємо перші два елементи з A в B
2. Для кожного елемента, що залишився в A: 
   - Обчислюємо **вартість** переміщення на правильну позицію в B
   - Вартість = ротації в A + ротації в B
   - Знаходимо елемент з **мінімальною вартістю**
   - Виконуємо переміщення (використовуючи комбіновані ротації `rr`/`rrr` коли можливо)
3. B тепер відсортований **за спаданням**

#### Фаза 2: Повернення в стек A

1. Знаходимо **максимальний** елемент в B
2. Ротуємо B, щоб максимум опинився зверху
3. Переміщуємо всі елементи з B в A
4. A тепер відсортований **за зростанням**

#### Фаза 3: Фінальна ротація

1. Знаходимо позицію мінімального елемента в A
2. Ротуємо A, щоб мінімум опинився зверху

### Розрахунок вартості

Для кожного елемента в A обчислюємо:
```
вартість = вартість_a + вартість_b
```

Де:
- `вартість_a` = ротації, щоб підняти елемент наверх A
- `вартість_b` = ротації, щоб підняти цільову позицію наверх B

**Оптимізація**:  Якщо обидві ротації в одному напрямку, використовуємо `rr` або `rrr` для зменшення загальної кількості ходів. 

### Складність

| Розмір стека | Операцій (приблизно) | Вимога |
|--------------|----------------------|--------|
| 3 | ≤ 3 | ≤ 3 |
| 5 | ≤ 8 | ≤ 12 |
| 100 | ≤ 550 | ≤ 700 |
| 500 | ≤ 5000 | ≤ 5500 |

---

## 🛠️ Інструкції

### Компіляція

```bash
# Скомпілювати push_swap
make

# Скомпілювати checker (бонус)
make bonus

# Очистити об'єктні файли
make clean

# Повне очищення (включаючи виконувані файли)
make fclean

# Перекомпілювати
make re
```

### Використання

#### push_swap

```bash
# Базове використання
./push_swap 3 2 1

# З більшою кількістю чисел
./push_swap 5 2 8 1 9 3 7 4 6

# Генерація випадкових чисел через shell
./push_swap $(seq 1 100 | shuf | xargs)

# Підрахунок операцій
./push_swap $(seq 1 100 | shuf | xargs) | wc -l
```

#### checker (бонус)

```bash
# Перевірити вивід push_swap
./push_swap 3 2 1 | ./checker 3 2 1
# Вивід:  OK

# Ручне введення (завершити через Ctrl+D)
./checker 3 2 1 0
rra
pb
sa
rra
pa
# Ctrl+D
# Вивід: OK або KO

# Тест з випадковими числами
ARG=$(seq 1 100 | shuf | xargs); ./push_swap $ARG | ./checker $ARG
```

### Тестування

```bash
# Тест 100 випадкових чисел
ARG=$(seq 1 100 | shuf | xargs)
echo "Операцій: $(./push_swap $ARG | wc -l)"
./push_swap $ARG | ./checker $ARG

# Тест 500 випадкових чисел
ARG=$(seq 1 500 | shuf | xargs)
echo "Операцій: $(./push_swap $ARG | wc -l)"
./push_swap $ARG | ./checker $ARG

# Запуск кількох тестів
for i in {1..10}; do
    ARG=$(seq 1 100 | shuf | xargs)
    OPS=$(./push_swap $ARG | wc -l)
    RESULT=$(./push_swap $ARG | ./checker $ARG)
    echo "Тест $i: $OPS операцій - $RESULT"
done
```

---

## 📁 Структура проєкту

```
push_swap/
├── push_swap.c           # Головна точка входу програми
├── checker_bonus. c       # Бонус:  програма checker
├── checker_utils_bonus.c # Бонус: парсинг інструкцій
├── parsing.c             # Парсинг та валідація аргументів
├── stack.c               # Створення та управління стеком
├── utils.c               # Допоміжні функції
├── check_utils.c         # Валідація введення
├── sorting.c             # Сортування малих стеків (≤5)
├── turk.c                # Алгоритм Turk sort
├── cost.c                # Розрахунок вартості
├── cost_utils.c          # Утиліти вартості
├── move.c                # Виконання переміщень
├── op_swap.c             # Операції sa, sb, ss
├── op_push.c             # Операції pa, pb
├── op_rotate.c           # Операції ra, rb, rr
├── op_reverse_rotate.c   # Операції rra, rrb, rrr
├── push_swap.h           # Заголовний файл
├── Makefile              # Конфігурація збірки
├── libft/                # Власна бібліотека C
└── README.md             # Цей файл
```

---

## 📚 Ресурси

### Документація та статті

- [Push_swap Tutorial](https://medium.com/@jamierobertdawson/push-swap-the-least-amount-of-moves-with-two-stacks-d1e76a71789a) — Детальний посібник по проєкту
- [Пояснення алгоритму Turk](https://medium.com/@ayogun/push-swap-c1f5d2d41e97) — Детальний розбір Turk sort
- [Візуалізатор](https://github.com/o-reo/push_swap_visualizer) — Корисний інструмент для налагодження

### Використання ШІ

У цьому проєкті використовувався ШІ (GitHub Copilot) для:

- **Навчання та розуміння**:  Пояснення концепції алгоритму Turk sort та стратегій оптимізації
- **Налагодження**: Виявлення проблем у конфігурації Makefile та помилок лінкування
- **Код-рев'ю**: Обговорення альтернативних підходів та найкращих практик
- **Документація**: Допомога у структуруванні цього README

Весь код був написаний, зрозумілий та протестований автором. ШІ слугував освітнім інструментом та помічником у написанні коду, аналогічно консультаціям з документацією або обговоренням з колегами.

---

## 👤 Автор

- **Логін**: vdarsuye
- **Кампус**: 42 Barcelona

---

## 📄 Ліцензія

Цей проєкт є частиною навчальної програми школи 42 та призначений для освітніх цілей. 
