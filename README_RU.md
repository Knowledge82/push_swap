*Этот проект создан в рамках учебной программы 42 пользователем vdarsuye*

# 🔄 Push_swap

## 📋 Описание

**Push_swap** — проект по алгоритмам сортировки, в котором нужно отсортировать стек целых чисел, используя ограниченный набор операций с минимальным количеством ходов.

Проект состоит из двух программ:
- **push_swap**:  Генерирует последовательность инструкций для сортировки стека
- **checker** (бонус): Проверяет, корректно ли последовательность инструкций сортирует стек

### Задача

У тебя есть два стека:
- **Стек A**: Содержит начальные неотсортированные числа
- **Стек B**: Изначально пуст, используется как вспомогательное хранилище

Можно использовать только эти операции:

| Операция | Описание |
|----------|----------|
| `sa` | Поменять местами первые 2 элемента стека A |
| `sb` | Поменять местами первые 2 элемента стека B |
| `ss` | Выполнить `sa` и `sb` одновременно |
| `pa` | Переместить верхний элемент из B в A |
| `pb` | Переместить верхний элемент из A в B |
| `ra` | Ротация стека A (первый элемент становится последним) |
| `rb` | Ротация стека B (первый элемент становится последним) |
| `rr` | Выполнить `ra` и `rb` одновременно |
| `rra` | Обратная ротация A (последний элемент становится первым) |
| `rrb` | Обратная ротация B (последний элемент становится первым) |
| `rrr` | Выполнить `rra` и `rrb` одновременно |

---

## 🧠 Алгоритм: Turk Sort

В этой реализации используется алгоритм **Turk Sort** — эффективный подход, минимизирующий общее количество операций.

### Как это работает

#### Фаза 1: Перемещение в стек B (по убыванию)

1. Перемещаем первые два элемента из A в B
2. Для каждого оставшегося элемента в A:
   - Вычисляем **стоимость** перемещения на правильную позицию в B
   - Стоимость = ротации в A + ротации в B
   - Находим элемент с **минимальной стоимостью**
   - Выполняем перемещение (используя комбинированные ротации `rr`/`rrr` когда возможно)
3. B теперь отсортирован **по убыванию**

#### Фаза 2: Возврат в стек A

1. Находим **максимальный** элемент в B
2. Ротируем B, чтобы максимум оказался наверху
3. Перемещаем все элементы из B в A
4. A теперь отсортирован **по возрастанию**

#### Фаза 3: Финальная ротация

1. Находим позицию минимального элемента в A
2. Ротируем A, чтобы минимум оказался наверху

### Расчёт стоимости

Для каждого элемента в A вычисляем:
```
стоимость = стоимость_a + стоимость_b
```

Где:
- `стоимость_a` = ротации, чтобы поднять элемент наверх A
- `стоимость_b` = ротации, чтобы поднять целевую позицию наверх B

**Оптимизация**: Если обе ротации в одном направлении, используем `rr` или `rrr` для уменьшения общего числа ходов.

### Сложность

| Размер стека | Операций (примерно) | Требование |
|--------------|---------------------|------------|
| 3 | ≤ 3 | ≤ 3 |
| 5 | ≤ 8 | ≤ 12 |
| 100 | ≤ 550 | ≤ 700 |
| 500 | ≤ 5000 | ≤ 5500 |

---

## 🛠️ Инструкции

### Компиляция

```bash
# Скомпилировать push_swap
make

# Скомпилировать checker (бонус)
make bonus

# Очистить объектные файлы
make clean

# Полная очистка (включая исполняемые файлы)
make fclean

# Перекомпилировать
make re
```

### Использование

#### push_swap

```bash
# Базовое использование
./push_swap 3 2 1

# С большим количеством чисел
./push_swap 5 2 8 1 9 3 7 4 6

# Генерация случайных чисел через shell
./push_swap $(seq 1 100 | shuf | xargs)

# Подсчёт операций
./push_swap $(seq 1 100 | shuf | xargs) | wc -l
```

#### checker (бонус)

```bash
# Проверить вывод push_swap
./push_swap 3 2 1 | ./checker 3 2 1
# Вывод: OK

# Ручной ввод (завершить через Ctrl+D)
./checker 3 2 1 0
rra
pb
sa
rra
pa
# Ctrl+D
# Вывод: OK или KO

# Тест со случайными числами
ARG=$(seq 1 100 | shuf | xargs); ./push_swap $ARG | ./checker $ARG
```

### Тестирование

```bash
# Тест 100 случайных чисел
ARG=$(seq 1 100 | shuf | xargs)
echo "Операций: $(./push_swap $ARG | wc -l)"
./push_swap $ARG | ./checker $ARG

# Тест 500 случайных чисел
ARG=$(seq 1 500 | shuf | xargs)
echo "Операций: $(./push_swap $ARG | wc -l)"
./push_swap $ARG | ./checker $ARG

# Запуск нескольких тестов
for i in {1.. 10}; do
    ARG=$(seq 1 100 | shuf | xargs)
    OPS=$(./push_swap $ARG | wc -l)
    RESULT=$(./push_swap $ARG | ./checker $ARG)
    echo "Тест $i: $OPS операций - $RESULT"
done
```

---

## 📁 Структура проекта

```
push_swap/
├── push_swap.c           # Главная точка входа программы
├── checker_bonus. c       # Бонус:  программа checker
├── checker_utils_bonus.c # Бонус:  парсинг инструкций
├── parsing. c             # Парсинг и валидация аргументов
├── stack.c               # Создание и управление стеком
├── utils.c               # Вспомогательные функции
├── check_utils.c         # Валидация ввода
├── sorting.c             # Сортировка малых стеков (≤5)
├── turk.c                # Алгоритм Turk sort
├── cost.c                # Расчёт стоимости
├── cost_utils.c          # Утилиты стоимости
├── move.c                # Выполнение перемещений
├── op_swap.c             # Операции sa, sb, ss
├── op_push.c             # Операции pa, pb
├── op_rotate.c           # Операции ra, rb, rr
├── op_reverse_rotate. c   # Операции rra, rrb, rrr
├── push_swap.h           # Заголовочный файл
├── Makefile              # Конфигурация сборки
├── libft/                # Собственная библиотека C
└── README.md             # Этот файл
```

---

## 📚 Ресурсы

### Документация и статьи

- [Push_swap Tutorial](https://medium.com/@jamierobertdawson/push-swap-the-least-amount-of-moves-with-two-stacks-d1e76a71789a) — Подробное руководство по проекту
- [Объяснение алгоритма Turk](https://medium.com/@ayogun/push-swap-c1f5d2d41e97) — Детальный разбор Turk sort
- [Визуализатор](https://github.com/o-reo/push_swap_visualizer) — Полезный инструмент для отладки

### Использование ИИ

В этом проекте использовался ИИ (GitHub Copilot) для:

- **Обучение и понимание**: Объяснение концепции алгоритма Turk sort и стратегий оптимизации
- **Отладка**:  Выявление проблем в конфигурации Makefile и ошибок линковки
- **Код-ревью**: Обсуждение альтернативных подходов и лучших практик
- **Документация**: Помощь в структурировании этого README

Весь код был написан, понят и протестирован автором. ИИ служил образовательным инструментом и помощником в написании кода, аналогично консультациям с документацией или обсуждениям с коллегами.

---

## 👤 Автор

- **Логин**: vdarsuye
- **Кампус**: 42 Barcelona

---

## 📄 Лицензия

Этот проект является частью учебной программы школы 42 и предназначен для образовательных целей.
